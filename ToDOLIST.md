# TODO 列表

## 当前计划
- [ ] 在 GTNH `2.9.0-beta-1` 客户端中验证 Waila 普通方块、实体、FMP part 和 XML 配置显示。

## 未来想法
- [ ] 后续可迁移到完整 manifest 驱动的新版 GTNH 构建模板。
- [ ] 后续可把 Nashorn 运行时依赖改为随 jar 分发或明确的安装依赖，避免离线客户端运行时下载。

## 已完成
- [x] 将 OmniOcular-Unofficial 构建依赖升级到 GTNH `2.9.0-beta-1` 版本。
- [x] 验证 `compileJava` 和 `build` 通过。

## 暂缓 / 拒绝
- 暂缓运行客户端实测：本次只完成本地构建和静态兼容检查。
